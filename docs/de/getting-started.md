# Erste Schritte

Um mit `custom_grid_env` zu beginnen, folge diesen Schritten:

1. **Installation**: Installiere das Paket via `pip install .`.
2. **Interface erstellen**: Nutze `AgentInterface` für eine einfache Interaktion.
3. **Agent implementieren**: Erstelle einen eigenen Agenten, der von der `Agent` Basisklasse erbt.

# Troubleshooting

Here you will find solutions to common problems.

## Pygame window does not open
Make sure that `render=True` is set in the `AgentInterface` and Pygame has been installed correctly.
